(() => {
  'use strict';
  const R=()=>window.RUMO,matBy=id=>R().state.materias.find(x=>x.id===id),topBy=id=>R().state.topicos.find(x=>x.id===id),dow=iso=>new Date(`${iso}T12:00:00`).getDay();
  let booting=null;
  function dates(start,count,allowed){const out=[];let d=start,i=0;while(out.length<count&&i<40){if(allowed.includes(dow(d)))out.push(d);d=R().addDays(d,1);i++}return out}
  function score(t,date){const m=matBy(t.materia_id),q=(t.acertos||0)+(t.erros||0),err=q?(t.erros||0)/q:0;let s=(Number(m?.peso)||1)*100+err*55;if(t.status==='nao_iniciado')s+=18;if(t.proxima_revisao&&t.proxima_revisao.slice(0,10)<=date)s+=65;return s}
  function choose(date,used,mode){let list=R().state.topicos.filter(t=>!used.has(t.id));if(mode==='revisao'){const due=list.filter(t=>t.proxima_revisao&&t.proxima_revisao.slice(0,10)<=date);if(due.length)list=due}else if(mode==='questoes'){const p=list.filter(t=>t.status!=='nao_iniciado'||(t.acertos||0)+(t.erros||0)>0);if(p.length)list=p}return [...list].sort((a,b)=>score(b,date)-score(a,date))[0]||null}
  function allocation(total){if(total<=60){const a=Math.round(total*.58);return[['estudo',a],['questoes',total-a]]}if(total<=120){const a=Math.round(total*.44),b=Math.round(total*.34);return[['estudo',a],['questoes',b],['revisao',total-a-b]]}const a=Math.round(total*.4),b=Math.round(total*.35);return[['estudo',a],['questoes',b],['revisao',total-a-b]]}
  async function loadTasks(){const r=R(),s=r.state;if(!s.user){s.tasks=[];renderGuest();return}const {data}=await r.sb.from('plano_tarefas').select('*').eq('user_id',s.user.id).eq('data',r.today()).order('criado_em');s.tasks=data||[];renderToday();r.emit('tasks',{tasks:s.tasks})}
  async function carryOver(){const r=R(),s=r.state,today=r.today(),allowed=s.goal.study_days?.length?s.goal.study_days:[1,2,3,4,5];if(!allowed.includes(dow(today)))return;const [{data:old},{count}]=await Promise.all([r.sb.from('plano_tarefas').select('*').eq('user_id',s.user.id).lt('data',today).eq('concluida',false).order('data').limit(3),r.sb.from('plano_tarefas').select('id',{count:'exact',head:true}).eq('user_id',s.user.id).eq('data',today).eq('concluida',false)]);const free=Math.max(0,3-(count||0));if(old?.length&&free)await Promise.all(old.slice(0,free).map(t=>r.sb.from('plano_tarefas').update({data:today}).eq('id',t.id)))}
  async function ensurePlan(force=false){const r=R(),s=r.state;if(!s.user||!s.goal||!s.userConcurso||!s.materias.length)return loadTasks();const start=r.today(),end=r.addDays(start,20);if(force)await r.sb.from('plano_tarefas').delete().eq('user_id',s.user.id).gte('data',start).eq('concluida',false);const {data:existing}=await r.sb.from('plano_tarefas').select('id,data').eq('user_id',s.user.id).gte('data',start).lte('data',end);const has=new Set((existing||[]).map(x=>x.data)),allowed=s.goal.study_days?.length?s.goal.study_days:[1,2,3,4,5],daily=Math.max(30,Math.min(300,Math.round((Number(s.goal.weekly_hours)||10)*60/allowed.length))),rows=[];for(const date of dates(start,12,allowed)){if(has.has(date))continue;const used=new Set();for(const [type,raw] of allocation(daily)){const t=choose(date,used,type);if(!t)continue;used.add(t.id);const m=matBy(t.materia_id);rows.push({user_id:s.user.id,user_concurso_id:s.userConcurso.id,materia_id:m?.id||null,topico_id:t.id,titulo:`${m?.nome||'Estudo'} — ${t.nome}`,tipo:type,data:date,minutos:Math.max(15,Math.round(raw/5)*5)})}}if(rows.length)await r.sb.from('plano_tarefas').insert(rows);await carryOver();await loadTasks()}
  function renderGuest(){const r=R(),box=r.qs('#study-list'),g=r.state.goal;if(!box||!g)return;const tpl=r.templates[r.templateKey(g,null)].slice(0,3),mins=Math.max(45,Math.round((Number(g.weekly_hours)||8)*60/Math.max(1,(g.study_days||[1,2,3,4,5]).length)));box.innerHTML=tpl.map((x,i)=>`<div class="study rumo-task"><div class="num">${i+1}</div><div><b>${r.escapeHTML(x[0])}</b><span>${r.escapeHTML(x[2][0])}</span></div><div class="time">${Math.max(15,Math.round(mins/tpl.length/5)*5)} min</div></div>`).join('');const h=r.qs('.focus-card h2');if(h)h.textContent=`${r.formatMinutes(mins)} de estudo hoje`}
  const typeLabel=t=>({estudo:'Aprender',questoes:'Praticar',revisao:'Revisar'})[t]||'Estudar';
  function renderToday(){const r=R(),s=r.state,box=r.qs('#study-list'),tasks=s.tasks||[];if(!box)return;const done=tasks.filter(t=>t.concluida).length,total=tasks.reduce((a,t)=>a+t.minutos,0);box.innerHTML=tasks.length?tasks.map((t,i)=>{const m=matBy(t.materia_id),tp=topBy(t.topico_id);return`<button class="study rumo-task ${t.concluida?'done':''}" data-task-id="${t.id}"><div class="num">${t.concluida?'✓':i+1}</div><div class="task-copy"><b>${r.escapeHTML(m?.nome||'Estudo')}</b><span>${r.escapeHTML(tp?.nome||t.titulo)}</span><span class="task-type">${typeLabel(t.tipo)}</span></div><div class="task-open"><div class="time">${t.minutos} min</div><span class="task-arrow">›</span></div></button>`}).join(''):'<div class="data-empty"><b>Nada pendente agora.</b>Seu plano está limpo para hoje.</div>';const h=r.qs('.focus-card h2'),p=r.qs('.focus-card .progress');if(h)h.textContent=`${r.formatMinutes(total)} de estudo hoje`;if(p)p.textContent=tasks.length?`${Math.round(done/tasks.length*100)}%`:'0%';r.qsa('[data-task-id]',box).forEach(b=>b.onclick=()=>{const t=tasks.find(x=>x.id===b.dataset.taskId);if(t)r.emit('open-session',{task:t})});ensureLog()}
  function ensureLog(){const r=R(),head=r.qs('.focus-card .card-heading');if(!head||r.qs('#log-study-v1'))return;const wrap=document.createElement('div');wrap.className='focus-actions-v1';const old=head.querySelector('.progress');if(old)wrap.appendChild(old);const b=document.createElement('button');b.id='log-study-v1';b.className='log-study-v1';b.textContent='+ Registrar estudo';b.onclick=()=>r.emit('open-manual-session',{});wrap.appendChild(b);head.appendChild(wrap)}
  async function analytics(){const r=R(),s=r.state;if(!s.user)return;const start=new Date();start.setDate(start.getDate()-6);start.setHours(0,0,0,0);const {data}=await r.sb.from('sessoes_estudo').select('minutos,questoes,acertos,estudado_em').eq('user_id',s.user.id).gte('estudado_em',start.toISOString()).order('estudado_em');const list=data||[],days=[];for(let i=6;i>=0;i--){const d=new Date();d.setDate(d.getDate()-i);days.push({k:d.toDateString(),m:0})}for(const x of list){const d=days.find(y=>y.k===new Date(x.estudado_em).toDateString());if(d)d.m+=x.minutos}const max=Math.max(30,...days.map(x=>x.m));r.qsa('.activity-chart .bar-day').forEach((el,i)=>{el.querySelector('i')?.style.setProperty('--h',`${Math.max(8,Math.round(days[i].m/max*100))}%`);el.title=`${days[i].m} min`});const total=list.reduce((a,x)=>a+x.minutos,0),active=days.filter(x=>x.m>0).length,q=list.reduce((a,x)=>a+(x.questoes||0),0),correct=list.reduce((a,x)=>a+(x.acertos||0),0),accuracy=q?Math.round(correct/q*100):0,mini=r.qsa('.activity-card .mini-stats>div');if(mini[0])mini[0].querySelector('b').textContent=r.formatMinutes(total);if(mini[1])mini[1].querySelector('b').textContent=active?r.formatMinutes(Math.round(total/active)):'0h';if(mini[2])mini[2].querySelector('b').textContent=active;const donut=r.qs('.performance-card .donut');if(donut){donut.style.background=`conic-gradient(var(--accent) 0 ${accuracy}%,var(--surface-3) ${accuracy}% 100%)`;donut.querySelector('b').textContent=q?`${accuracy}%`:'—'}const met=r.qsa('.performance-card .metric b');if(met[0])met[0].textContent=q;if(met[1])met[1].textContent=list.length?`${list.length} sessões`:'Sem dados ainda';const goalMin=(Number(s.goal?.weekly_hours)||10)*60,hours=r.qs('.week-card .hours');if(hours)hours.innerHTML=`${r.formatMinutes(total)} <span>/ ${Number(s.goal?.weekly_hours||10).toLocaleString('pt-BR')}h</span>`;const bar=r.qs('.week-card .goal-bar i');if(bar)bar.style.width=`${Math.min(100,Math.round(total/goalMin*100))}%`}
  async function objective(){const r=R(),s=r.state;if(!s.goal)return;const divs=r.qsa('.objective-strip>div');if(divs[0])divs[0].querySelector('b').textContent=s.goal.objective_name;const left=r.daysUntil(s.goal.prova_data);if(divs[1])divs[1].querySelector('b').textContent=left===null?'A definir':left===0?'É hoje!':left>0?`${left} dias`:'Realizada';if(divs[2]){divs[2].querySelector('small').textContent='RITMO PLANEJADO';divs[2].querySelector('b').textContent=`${Number(s.goal.weekly_hours||10).toLocaleString('pt-BR')}h/sem`}const hero=r.qs('.hero-home p');if(hero)hero.textContent=`Seu foco é ${s.goal.objective_name}. O RUMO ajusta o próximo passo ao seu ritmo real.`}
  async function goalPage(){const r=R(),s=r.state,grid=r.qs('#meu .setup-grid');if(!grid||!s.goal)return;let latest=null;if(s.user){const {data}=await r.sb.from('edital_imports').select('file_name,subjects_found,created_at').eq('user_id',s.user.id).order('created_at',{ascending:false}).limit(1).maybeSingle();latest=data||null}const left=r.daysUntil(s.goal.prova_data);grid.className='goal-dashboard';grid.innerHTML=`<article class="goal-main-card"><div class="eyebrow">OBJETIVO ATUAL</div><h2>${r.escapeHTML(s.goal.objective_name)}</h2><p>${s.goal.cargo?r.escapeHTML(s.goal.cargo):'Preparação personalizada'} · ${latest?`Edital importado · ${latest.subjects_found} matérias`:'Plano-base RUMO'}</p><div class="goal-kpis-v1"><div><b>${left===null?'—':Math.max(0,left)}</b><span>dias até a prova</span></div><div><b>${Number(s.goal.weekly_hours||10).toLocaleString('pt-BR')}h</b><span>por semana</span></div><div><b>${(s.goal.study_days||[]).length}</b><span>dias de estudo</span></div></div><button class="secondary" id="goal-adjust-v1" style="margin-top:16px">Ajustar objetivo e rotina</button></article><article class="goal-side-card"><div class="eyebrow">EDITAL INTELIGENTE</div><h3>Transforme o PDF em plano.</h3><p>O arquivo é lido no seu navegador e convertido em matérias e tópicos.</p><label class="edital-drop"><input id="edital-file-v1" type="file" accept="application/pdf"><b>Selecionar edital em PDF</b><span>PDFs com texto selecionável funcionam melhor.</span></label><div class="edital-status" id="edital-status-v1">${latest?`Último: ${r.escapeHTML(latest.file_name)}`:'Nenhum edital importado ainda.'}</div><button class="secondary" id="regenerate-plan-v1" style="width:100%;margin-top:12px">Recalcular próximos dias</button></article><article class="subjects-card"><div class="eyebrow">MAPA DO CONTEÚDO</div><h3>Matérias e domínio</h3><div class="subjects-list">${s.materias.map(m=>{const ts=s.topicos.filter(t=>t.materia_id===m.id),pct=ts.length?Math.round(ts.filter(t=>t.status==='dominado').length/ts.length*100):m.progresso||0;return`<div class="subject-row"><div><b>${r.escapeHTML(m.nome)}</b><small>${ts.length} tópicos</small></div><div class="subject-bar"><i style="width:${pct}%"></i></div><b>${pct}%</b><span class="priority-pill ${m.prioridade}">${m.prioridade}</span></div>`}).join('')}</div></article>`;r.qs('#goal-adjust-v1')?.addEventListener('click',()=>r.qs('#personalize-btn')?.click());r.qs('#regenerate-plan-v1')?.addEventListener('click',async()=>{r.toast('Recalculando...');await ensurePlan(true);r.toast('Plano atualizado')});r.emit('goal-page',{latest})}
  async function boot(){if(booting)return booting;const r=R();if(!r?.state.ready)return;booting=Promise.all([ensurePlan(),analytics(),objective()]).finally(()=>{booting=null});return booting}
  document.addEventListener('rumo:context',boot);document.addEventListener('rumo:structure',()=>{if(document.querySelector('#meu.page.active'))goalPage()});document.addEventListener('rumo:tab',e=>{if(e.detail?.id==='meu')goalPage()});document.addEventListener('rumo:session-saved',async()=>{await R().refreshStructure();await Promise.all([loadTasks(),analytics(),objective()]);if(document.querySelector('#meu.page.active'))goalPage()});document.addEventListener('rumo:plan-refresh',()=>ensurePlan(true));
  window.RUMO_ENGINE={ensurePlan,loadTasks,renderAnalytics:analytics,renderGoalPage:goalPage};
})();